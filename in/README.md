# in/ — project copies, read-only

Drop copies of Logic projects here to run the tools against. Nothing in this directory is ever
written by the tools; it is input only, so the originals in Logic's own library are never at
risk.

Gitignored except this file: a fresh clone gets an empty directory, and whatever you put here
stays on your machine.
